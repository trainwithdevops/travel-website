import React from 'react';

const FindMembers = () => {
    return (
        <div>
            <h1>Find Members</h1>
            <p>Find and connect with other travellers.</p>
            {/* Implement filtering options */}
        </div>
    );
};

export default FindMembers;
